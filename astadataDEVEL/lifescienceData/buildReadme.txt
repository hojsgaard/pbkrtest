to check the package run

Rcmd check dataRep


to
 -build the package
 -put it onto the web doing the following:

   execute from the  dos-command line
  
    build.bat

    build.bat calls

     build.R:   the packages are compiled for several versions
     build_winscp.txt the packages are web transfered to packges of the different version
              you must possibly edit build_wincp to include newe versions of R eg. R-2.11


