% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pbkr_simulate.R
\name{simulate.gls}
\alias{simulate.gls}
\alias{simulate.lme}
\title{Simulate Response Vectors from Fitted nlme Models}
\usage{
\method{simulate}{gls}(object, nsim = 1, seed = NULL, ...)

\method{simulate}{lme}(object, nsim = 1, seed = NULL, ...)
}
\arguments{
\item{object}{A fitted \code{gls} or \code{lme} object (from the \code{nlme} package).}

\item{nsim}{Number of simulated datasets to generate (default is 1).}

\item{seed}{Optional random seed for reproducibility.}

\item{...}{Additional arguments (currently ignored).}
}
\value{
A data frame with \code{nsim} simulated response vectors as columns. Each column represents one simulated replicate of the response under the model's implied distribution.
}
\description{
These methods generate simulated response vectors from
the multivariate normal distribution implied by a fitted
\code{gls} or \code{lme} model. The simulation accounts for the
fitted mean and covariance structure of the model.
}
\section{Details}{
 For \code{gls} objects, the mean vector is the
fitted values and the covariance matrix is obtained via
\code{cov_matrix()}.  For \code{lme} objects, the mean vector
is the level-0 (population) predicted values and the covariance
matrix is also obtained via \code{cov_matrix()}.
}

\examples{
if (require(nlme)) {
  ## Example for gls
  fit_gls <- gls(distance ~ age, data = Orthodont)
  sims_gls <- simulate(fit_gls, nsim = 5, seed = 123)
  head(sims_gls)

  ## Example for lme
  fit_lme <- lme(distance ~ age, random = ~ 1 | Subject, data = Orthodont)
  sims_lme <- simulate(fit_lme, nsim = 5, seed = 123)
  head(sims_lme)
}

}
